# --- Dependencies (Quan trọng nhất) ---
# Dòng này sẽ bỏ qua node_modules ở tất cả mọi nơi (root, backend, frontend)
node_modules/

# --- Bảo mật (Bắt buộc) ---
# Không bao giờ up file chứa mật khẩu lên git
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# --- Build Outputs (Frontend) ---
# Bỏ qua thư mục build của React
build/
dist/

# --- Logs ---
npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs
*.log

# --- OS System Files (File rác của hệ điều hành) ---
.DS_Store
Thumbs.db

# --- IDE (Cấu hình của VS Code...) ---
.vscode/
.idea/
*.swp
*.swo

# --- Docker ---
# Nếu bạn có mount dữ liệu DB ra ngoài thì nên ignore nó
mongo-data/